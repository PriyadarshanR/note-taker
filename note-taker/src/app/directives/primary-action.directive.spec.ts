import { PrimaryActionDirective } from './primary-action.directive';

describe('PrimaryActionDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryActionDirective();
    expect(directive).toBeTruthy();
  });
});
